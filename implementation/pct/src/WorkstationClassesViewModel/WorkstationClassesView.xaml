<UserControl x:Class="Your.WorkstationClassesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="auto"  Width="auto">
    <Grid Margin="10,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ListView x:Name="lstNames" Margin="5,5,35,5" Grid.Column="1" Grid.Row="0" Grid.RowSpan="1" ItemsSource="{Binding ObservableWorkstationClass}" SelectedItem="{Binding SelectedProcess, Mode=TwoWay}">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Visibility" Value="Visible" />
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView x:Name="grdNames">
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding WC_name}"/>
                    <GridViewColumn Header="Type"  DisplayMemberBinding="{Binding WC_type}"/>
                    <GridViewColumn Header="Handling type"  DisplayMemberBinding="{Binding WC_handlingType}"/>
                    <GridViewColumn Header="Process" DisplayMemberBinding="{Binding ProcessRef.P_name}"/>
                    <GridViewColumn Header="Secondary activity" DisplayMemberBinding="{Binding SecondaryactivityRef.SC_objName}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0">
            <TextBlock x:Name="lblpc_objName" FontFamily="Verdana" FontWeight="Bold" Margin="5" Text="Name:"></TextBlock>
            <TextBox x:Name="txtpc_objName" Text="{Binding TobeEditedItem.WC_name}"></TextBox>
            <TextBlock x:Name="lblpc_objDes" FontFamily="Verdana" FontWeight="Bold" Margin="5" Text="Description:"></TextBlock>
            <TextBox x:Name="txtpc_objDes" Text="{Binding TobeEditedItem.WC_type}"></TextBox>
            <TextBlock x:Name="lblPC_ComID" FontFamily="Verdana" FontWeight="Bold" Margin="5" Text="Communication ID:"></TextBlock>
            <TextBox x:Name="txtPC_ComID" Text="{Binding TobeEditedItem.WC_handlingType}"></TextBox>
            <TextBlock x:Name="lblP_ProdArea" FontFamily="Verdana" FontWeight="Bold" Margin="5" Text="Process:"></TextBlock>
            <ComboBox FontFamily="Arial" FontSize="11" x:Name="cb_Prodref" SelectedItem="null" Text="{Binding TobeEditedItem.EditProcessRef.editP_name}" ItemsSource="{Binding ObservableProdArea}" DisplayMemberPath="P_name"></ComboBox>
            <TextBlock x:Name="lblPC_InBuffer" FontFamily="Verdana" FontWeight="Bold" Margin="5" Text="Secondary Activity:"></TextBlock>
            <ComboBox FontFamily="Arial" FontSize="11" x:Name="cb_Inbuffer" SelectedIndex="1" Text="{Binding TobeEditedItem.EditsecondaryactivityRef.editSC_name}" ItemsSource="{Binding ObservableBuffer}" DisplayMemberPath="SC_name"></ComboBox>

        </StackPanel>
        <StackPanel Grid.Row="1" Grid.RowSpan="1" Grid.Column="0" Grid.ColumnSpan="2" Background="#fafafa" Width="auto">
            <Button Width="auto" Height="auto" Margin="27,5,27.8,5" Grid.Row="1" x:Name="btnUpdate" Command="{Binding UpdateCommand}" Content="Update selected item"/>
            <Button Width="auto" Height="auto" Margin="27,5,27.8,5" Grid.Row="1" x:Name="btnAdd" Command="{Binding AddCommand}" Content="Add a new Process"/>
            <Button Width="auto" Height="auto" Margin="27,5,27.8,5" Grid.Row="1" x:Name="btnDelete" Content="Delete selected Item" Command="{Binding DeleteCommand}"/>
        </StackPanel>


    </Grid>
</UserControl>
